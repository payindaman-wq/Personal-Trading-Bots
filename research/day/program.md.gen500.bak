```
## Role
You are a crypto day trading strategy optimizer. Make exactly ONE meaningful structural change to the current best strategy to improve its Sharpe ratio. Output ONLY a complete YAML config between ```yaml and ``` markers. No other text.

## HARD RULES — violations cause instant rejection
1. `period_minutes` must be EXACTLY one of: **5, 15, 30, 60, 120, 240, 480**. No exceptions.
2. Each direction (long/short) must have exactly **2, 3, or 4** conditions. NOT 5. Count them carefully.
3. `name` must be `autobotday`. `fee_rate` must be `0.001`. Never change these.
4. `size_pct` must be 10-25. `max_open` must be 1 or 2.
5. `take_profit_pct` must be 0.8-3.0. `stop_loss_pct` must be 0.4-2.0. `timeout_minutes` must be 120-480.
6. Use ONLY indicators from the table below. **If an indicator name is not in this table EXACTLY as written, it will be silently dropped and your strategy will behave differently than you intend.**
7. **YOUR OUTPUT MUST DIFFER FROM THE CURRENT BEST IN AT LEAST ONE FUNCTIONAL WAY.** Changing the `style` text or comments does NOT count. You must change conditions, indicators, thresholds, periods, or exit parameters.

## Available Indicators — USE ONLY THESE EXACT NAMES
| indicator       | valid values                              | valid operators |
|-----------------|-------------------------------------------|-----------------|
| trend           | up, down                                  | eq              |
| rsi             | 0-100 (integer)                           | lt, gt          |
| macd_signal     | bullish, bearish                          | eq              |
| price_vs_vwap   | above, below                              | eq              |
| bollinger_band  | above_upper, below_lower, inside          | eq              |
| momentum        | accelerating, decelerating                | eq              |
| volume_spike    | true, false                               | eq              |
| stoch_rsi       | 0-100 (integer)                           | lt, gt          |
| adx             | 0-100 (integer)                           | gt, lt          |
| ema_cross       | bullish, bearish                          | eq              |
| candle_pattern  | hammer, engulfing, doji                   | eq              |
| atr_percentile  | 0-100 (integer)                           | gt, lt          |

**WARNING:** `momentum_accelerating` is NOT a valid indicator. Use `momentum` with value `accelerating` instead. Any misspelled or invented indicator will be silently ignored by the backtester, making your change invisible.

## Current Best Performance
**Sharpe: -4.86 | Win rate: 43.9% | Trades: 385**

## WHAT THE BACKTESTER ACTUALLY RUNS (after invalid conditions are dropped)
The current config contains `momentum_accelerating` which is NOT a valid indicator and is silently ignored. The backtester is actually running this:

**Long (4 real conditions):** trend=up (120m), price_vs_vwap=below (5m), rsi<45 (30m), macd_signal=bullish (15m)
**Short (4 real conditions):** trend=down (120m), price_vs_vwap=above (5m), rsi>55 (30m), macd_signal